namespace Sandbox.k.Views {
    public class ClosableViewBase : ViewBase {
        // [Property] private Button[] _closeButtons;

        // protected override void OnEnable() {
        //     base.OnEnable();
        //     foreach (var button in _closeButtons) button.onClick.AddListener(DisableView);
        // }
        //
        // private void DisableView() {
        //     SetActive(false);
        // }
        //
        // protected override void OnDisable() {
        //     base.OnDisable();
        //     foreach (var button in _closeButtons) button.onClick.RemoveListener(DisableView);
        // }
    }
}
